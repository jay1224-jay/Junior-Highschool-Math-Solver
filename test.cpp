#include <iostream>
#include <array>
#include <string>
using namespace std;

#define MAX(a, b) ((a<b) ? (b) : (a))
#define MIN(c, d) ((c>d) ? (d) : (c))


int main()
{

    int a;

    cin >> a;

    cout << "you type " << a << endl;

    return 0; 
}


